import { Anton, Mulish } from '@next/font/google'

export const titleFont = Anton({
  subsets: ['latin'],
  weight: '400'
})

export const bodyFont = Mulish({
  subsets: ['latin'],
  weight: '400'
})
